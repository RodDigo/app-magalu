services:
  mysql:
    container_name: 'db-magalu'
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=db-magalu'
      - 'MYSQL_PASSWORD=mydatabase'
      - 'MYSQL_ROOT_PASSWORD=mydatabase'
      - 'MYSQL_USER=mydatabase'
    ports:
      - '3306:3306'
